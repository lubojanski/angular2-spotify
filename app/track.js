"use strict";
var Track = (function () {
    function Track(id, name, track_number, preview_url, artists) {
        this.id = id;
        this.name = name;
        this.track_number = track_number;
        this.preview_url = preview_url;
    }
    return Track;
}());
exports.Track = Track;
//# sourceMappingURL=track.js.map